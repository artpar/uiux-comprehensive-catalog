{
  "id": "checkbox-group-vs-multi-select",
  "title": "Checkbox group vs multi-select",
  "summary": "Use checkbox groups when a small set of independent choices should stay visible. Use multi-select when the option set is large enough that compact picking is necessary.",
  "patternIds": ["checkbox-group", "multi-select"],
  "decisionRules": [
    "Prefer checkbox groups when a short set of independent choices can remain visible and users need to compare or toggle several options directly.",
    "Prefer multi-select when the option set is long, searchable, dynamic, or compact enough that selected values need chip-style review outside the open list.",
    "Keep selected values reviewable before the user commits the form; for checkbox groups the list itself can be the review surface, while multi-select needs an explicit selected-value surface."
  ],
  "failureModes": [
    "Collapsing a short critical choice set into a hidden control.",
    "Using many checkboxes for a long list without search or grouping."
  ],
  "sources": [
    {
      "id": "govuk-checkboxes-component",
      "note": "GOV.UK checkbox guidance documents visible one-or-more selections, fieldset/legend grouping, hints, errors, and independent checked values."
    },
    {
      "id": "uswds-checkbox-component",
      "note": "USWDS checkbox guidance supports multiple visible choices, selectable labels, vertical layout, and fieldset/legend grouping."
    },
    {
      "id": "wai-aria-apg-checkbox",
      "note": "APG checkbox guidance defines checked states, labels, grouped descriptions, and Space-key toggling."
    },
    {
      "id": "wai-aria-apg-listbox",
      "note": "APG listbox guidance is the contrast source for longer or custom multi-selection widgets with selected-state and keyboard requirements."
    },
    {
      "id": "carbon-dropdown-multiselect",
      "note": "Carbon multiselect guidance documents filterable compact picking, checkbox options, persistent menus, selected tags, and clear-all behavior."
    },
    {
      "id": "calcite-combobox-component",
      "note": "Calcite combobox guidance documents multi-selection chips, filtering, removal, validation, and selected item state."
    }
  ],
  "lastVerified": "2026-06-08"
}
